name: CI

on:
  push:
    branches:
      - master
      - '[0-9]'
  pull_request:
    branches:
      - master
      - '[0-9]'
  workflow_dispatch:

jobs:
  ci:
    name: CI
    uses: dynamic/silverstripe-ci/.github/workflows/ci.yml@v4
