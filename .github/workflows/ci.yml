name: CI

on:
  push:
    branches:
      - '**'
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  ci:
    name: CI
    uses: dynamic/silverstripe-ci/.github/workflows/ci.yml@main
